[package]
name = "feff10-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
globset = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
tempfile = "3.13"
