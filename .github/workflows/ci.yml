name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install gfortran
        run: sudo apt-get update && sudo apt-get install -y gfortran make

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Clippy
        env:
          FEFF_FC: gfortran
        run: cargo clippy --workspace --all-targets -- -D warnings

  build-and-test:
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            compiler: gfortran
            install: sudo apt-get update && sudo apt-get install -y gfortran
          - os: ubuntu-latest
            compiler: ifx
            install: ""  # handled by setup-fortran action
            experimental: true
          - os: ubuntu-latest
            compiler: flang
            install: ""  # handled by LLVM apt repo setup below
            experimental: true
          - os: macos-latest
            compiler: gfortran
            install: brew install gcc
            experimental: true
          - os: windows-latest
            compiler: gfortran
            install: ""  # handled by msys2 action

    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    continue-on-error: ${{ matrix.experimental || false }}

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ runner.os == 'Windows' && 'x86_64-pc-windows-gnu' || '' }}

      - name: Cache cargo registry and build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-${{ matrix.compiler }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ matrix.compiler }}-cargo-

      # --- Compiler installation ---

      - name: Install gfortran (Linux)
        if: matrix.compiler == 'gfortran' && runner.os == 'Linux'
        run: ${{ matrix.install }}

      - name: Install gfortran (macOS) and find versioned binary
        if: matrix.compiler == 'gfortran' && runner.os == 'macOS'
        id: macos-gfortran
        run: |
          ${{ matrix.install }}
          # brew installs gfortran as gfortran-NN, find the versioned name
          FC=$(ls /opt/homebrew/bin/gfortran-* /usr/local/bin/gfortran-* 2>/dev/null | head -1 || true)
          if [ -z "$FC" ]; then
            FC=$(which gfortran 2>/dev/null || true)
          fi
          if [ -z "$FC" ]; then
            echo "ERROR: gfortran not found"
            exit 1
          fi
          echo "fc=$FC" >> "$GITHUB_OUTPUT"
          echo "Found Fortran compiler: $FC"

      - name: Install gfortran (Windows)
        if: matrix.compiler == 'gfortran' && runner.os == 'Windows'
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: false
          install: mingw-w64-x86_64-gcc-fortran make

      - name: Install Intel Fortran (ifx)
        if: matrix.compiler == 'ifx'
        uses: fortran-lang/setup-fortran@v1
        with:
          compiler: intel
          version: '2024.0'

      - name: Resolve ifx compiler path
        if: matrix.compiler == 'ifx'
        id: ifx-path
        run: |
          FC=$(which ifx)
          echo "fc=$FC" >> "$GITHUB_OUTPUT"
          echo "Found ifx at: $FC"

      - name: Install LLVM Flang
        if: matrix.compiler == 'flang'
        id: flang-path
        run: |
          # Add official LLVM apt repository for Ubuntu
          wget -qO- https://apt.llvm.org/llvm-snapshot.gpg.key | sudo tee /etc/apt/trusted.gpg.d/apt.llvm.org.asc
          CODENAME=$(lsb_release -cs)
          echo "deb http://apt.llvm.org/$CODENAME/ llvm-toolchain-$CODENAME-20 main" | sudo tee /etc/apt/sources.list.d/llvm-20.list
          sudo apt-get update
          sudo apt-get install -y flang-20
          # flang-20 installs the binary as flang-new-20
          FC=$(which flang-new-20)
          echo "fc=$FC" >> "$GITHUB_OUTPUT"
          echo "Found flang at: $FC"

      # --- Build ---

      - name: Build (Linux - gfortran)
        if: matrix.compiler == 'gfortran' && runner.os == 'Linux'
        env:
          FEFF_FC: gfortran
        run: cargo build --workspace

      - name: Build (Linux - ifx)
        if: matrix.compiler == 'ifx' && runner.os == 'Linux'
        env:
          FEFF_FC: ${{ steps.ifx-path.outputs.fc }}
        run: cargo build --workspace

      - name: Build (Linux - flang)
        if: matrix.compiler == 'flang' && runner.os == 'Linux'
        env:
          FEFF_FC: ${{ steps.flang-path.outputs.fc }}
        run: cargo build --workspace

      - name: Build (macOS)
        if: runner.os == 'macOS'
        env:
          FEFF_FC: ${{ steps.macos-gfortran.outputs.fc }}
        run: |
          FC_DIR=$(dirname "${{ steps.macos-gfortran.outputs.fc }}")
          LIB_DIR=$(cd "$FC_DIR/../lib/gcc/current" 2>/dev/null && pwd || echo "")
          if [ -z "$LIB_DIR" ]; then
            LIB_DIR=$("${{ steps.macos-gfortran.outputs.fc }}" -print-file-name=libgfortran.dylib | xargs dirname)
          fi
          echo "LIBRARY_PATH=$LIB_DIR" >> "$GITHUB_ENV"
          export LIBRARY_PATH="$LIB_DIR"
          cargo build --workspace

      - name: Build (Windows)
        if: runner.os == 'Windows'
        shell: msys2 {0}
        env:
          FEFF_FC: gfortran
        run: |
          export PATH="/c/Users/runneradmin/.cargo/bin:/mingw64/bin:$PATH"
          cargo build --workspace --target x86_64-pc-windows-gnu

      # --- Tests ---

      - name: Unit tests (Linux - gfortran)
        if: matrix.compiler == 'gfortran' && runner.os == 'Linux'
        env:
          FEFF_FC: gfortran
        run: cargo test --workspace --lib

      - name: Unit tests (Linux - ifx)
        if: matrix.compiler == 'ifx' && runner.os == 'Linux'
        env:
          FEFF_FC: ${{ steps.ifx-path.outputs.fc }}
        run: cargo test --workspace --lib

      - name: Unit tests (Linux - flang)
        if: matrix.compiler == 'flang' && runner.os == 'Linux'
        env:
          FEFF_FC: ${{ steps.flang-path.outputs.fc }}
        run: cargo test --workspace --lib

      - name: Unit tests (macOS)
        if: runner.os == 'macOS'
        env:
          FEFF_FC: ${{ steps.macos-gfortran.outputs.fc }}
        run: cargo test --workspace --lib

      - name: Unit tests (Windows)
        if: runner.os == 'Windows'
        shell: msys2 {0}
        run: |
          export PATH="/c/Users/runneradmin/.cargo/bin:/mingw64/bin:$PATH"
          cargo test --workspace --lib --target x86_64-pc-windows-gnu

      - name: Integration tests (Linux - gfortran)
        if: matrix.compiler == 'gfortran' && runner.os == 'Linux'
        env:
          FEFF_FC: gfortran
        run: cargo test -p feff10 --test integration -- --test-threads=1
        timeout-minutes: 30

      - name: Integration tests (Linux - ifx)
        if: matrix.compiler == 'ifx' && runner.os == 'Linux'
        env:
          FEFF_FC: ${{ steps.ifx-path.outputs.fc }}
        run: cargo test -p feff10 --test integration -- --test-threads=1
        timeout-minutes: 30

      - name: Integration tests (Linux - flang)
        if: matrix.compiler == 'flang' && runner.os == 'Linux'
        env:
          FEFF_FC: ${{ steps.flang-path.outputs.fc }}
        run: cargo test -p feff10 --test integration -- --test-threads=1
        timeout-minutes: 30

      - name: Integration tests (macOS)
        if: runner.os == 'macOS'
        env:
          FEFF_FC: ${{ steps.macos-gfortran.outputs.fc }}
        run: cargo test -p feff10 --test integration -- --test-threads=1
        timeout-minutes: 30

      # Windows integration tests skipped: no fork() means Fortran global
      # state is not isolated between stages (causes "already allocated" errors)
